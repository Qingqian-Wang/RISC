import java.io.IOException;

import org.junit.jupiter.api.Test;

public class PlayerInfoTest {
	@Test
	public void test1() throws IOException {
//		Socket temSocket = new Socket("localhost", 0);
//		PlayerInfo temp1 = new PlayerInfo(temSocket, 3);
//		assertEquals(temp1.getPlayerID(), 3);
////		assertEquals(temp1.getPlayerSocket(), temSocket);
////		assertEquals(temp1., null);
		
	}
}
