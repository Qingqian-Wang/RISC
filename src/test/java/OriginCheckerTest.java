import org.junit.jupiter.api.Test;

import java.util.ArrayList;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class OriginCheckerTest {
//    @Test
//    public void testCorrectness(){
//        BasicChecker checker = new OriginChecker(null);
//        Territory t1 = new Territory("PlaceA", 1);
//        t1.setUnit(20);
//        Territory t2 = new Territory("PlaceB", 2);
//        ArrayList<Territory> map = new ArrayList<>();
//        map.add(t1);
//        map.add(t2);
//        Behavior b1 = new Behavior(t1,t2,10,1,"Move");
//        assertEquals(null,checker.checkBehavior(b1,map));
//        Behavior b2 = new Behavior(t1,t2,10,2,"Move");
//        assertEquals("the origin territory is not belong to player2",checker.checkBehavior(b2,map));
//        Behavior b3 = new Behavior(t1,t2,50,1,"Move");
//        assertEquals("use Unit larger than it has! it has 20 but use 50",checker.checkBehavior(b3,map));
//    }
}
